Add fade-in animations to each section
Tooltip appears briefly in wrong position until snapping into place (it needs to useLayoutEffect instead of useEffect. Maybe contribute/open issue in react-bootstrap? (see if it's fixed in their v3))

Understand why setRequestLocale, async/await, use/getTranslations, NextIntlClientProvider, params, does 'use client' cascade?, etc.

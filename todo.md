Understand why setRequestLocale, async/await, use/getTranslations, NextIntlClientProvider, params, does 'use client' cascade?, etc.

See if it is possible to redirect pt to pt-br and en-us/en-uk/en-whatever to en (also change the path instead of keeping the wrong locale in it)

Do I need to add arias? (Bootstrap has more arias than react-bootstrap. See accessibility score in lighthouse)

Add bootstrap button variants for contacts

Add tooltip saying which social media it is

Check labels like the close button one that are set by React Bootstrap and also need to be localized

LATER
Check image alts
Optimize images?
Scan in lighthouse
